// Цвета и пороги рендеринга карты (RGB-триплеты)
export const FORT_BORDER: [number, number, number] = [222, 214, 196]; // каменная граница
export const WATER: [number, number, number] = [58, 96, 140];
export const WAR: [number, number, number] = [225, 36, 26]; // линия фронта

// метки стран/людей показываем, когда приблизились вдвое от обзорного зума;
// «корм» — только если он огромный или приближение очень сильное
export const LABEL_ZOOM_MUL = 2;
export const FOOD_LABEL_MUL = 5;
export const FOOD_LABEL_CELLS = 4000;

// Цифры уровня зданий — важная информация (уровень штаба задаёт сопротивление
// 1:5/1:7/1:10, уровень города — прибавку к потолку армии), поэтому они видны
// почти на всех зумах и гаснут только на обзорном виде «вся карта целиком»,
// где текст всё равно превратился бы в кашу.
export const BADGE_ZOOM_MUL = 1.25;

// цвета нейтральной местности по типу почвы — как на классических картах мира
export const TERRAIN: Record<number, [number, number, number]> = {
  1: [150, 182, 120], // трава
  2: [214, 200, 150], // песок
  3: [176, 168, 150], // камень
  4: [246, 247, 246], // снег
};

// Размер модельки дрона (в пикселях), до которого включительно он рисуется точкой:
// на отдалённой карте детали самолётика не видны, а стоят полного набора путей.
export const DRONE_DOT_SIZE_MAX = 5;
// Радиус трейд-судна (в пикселях), до которого включительно оно рисуется точкой:
// на отдалённой карте белая обводка кружка всё равно не различима.
export const SHIP_DOT_RADIUS_MAX = 3;

// Боевые корабли: с какого зума рисуем «подробно» — белую иконку поверх кружка и
// знаки различия звания. Ниже порога иконка (drawImage 64-пиксельного спрайта на
// КАЖДЫЙ корабль каждый кадр) не читается и только засоряет обзорный вид, а полоски
// звания уходят в подпиксель. Порог тот же, что у зданий (shapeMode), и совпадает с
// зумом, на котором радиус корабля упирается в свой минимум (11 px).
export const WARSHIP_DETAIL_ZOOM = 2.4;
